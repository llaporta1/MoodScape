import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.laurenlaporta.MoodScape',
  appName: 'MoodScape',
  webDir: 'public'
};

export default config;
